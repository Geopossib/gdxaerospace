# Data

Reference datasets used for validation (e.g. tabulated atmosphere or
airfoil data to check against) land here as they're added. No package
currently ships bundled data files — `aerocalc.Atmosphere` and the
airfoil/lamina property functions are all computed analytically or from
in-code constants, cited to their sources in each module's docstring
rather than from an external data file.
