[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "plasmaspace"
version = "0.1.0"
description = "Preliminary spacecraft-plasma interaction estimates (floating potential, sheath) for the GDX Aerospace ecosystem"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "GDX Tech Co. Ltd" }]
dependencies = [
    "numpy>=1.26",
    "aerocalc",
    "plasmathrust",
]

[project.optional-dependencies]
test = ["pytest>=8.0"]

[tool.hatch.build.targets.wheel]
packages = ["src/plasmaspace"]

[tool.uv.sources]
aerocalc = { workspace = true }
plasmathrust = { workspace = true }
