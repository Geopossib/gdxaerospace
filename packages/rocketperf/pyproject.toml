[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "rocketperf"
version = "0.1.0"
description = "Rocket engine performance relations (Isp, c*, CF, ideal rocket equation) for the GDX Aerospace ecosystem"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "GDX Tech Co. Ltd" }]
dependencies = [
    "numpy>=1.26",
    "aerocalc",
]

[project.optional-dependencies]
test = ["pytest>=8.0"]

[tool.hatch.build.targets.wheel]
packages = ["src/rocketperf"]

[tool.uv.sources]
aerocalc = { workspace = true }
aerounits = { workspace = true }
compressibleflow = { workspace = true }
