[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "aircraftsim"
version = "0.1.0"
description = "A simple 6-DOF aircraft flight simulator built on flightdyn, for the GDX Aerospace ecosystem"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "GDX Tech Co. Ltd" }]
dependencies = [
    "numpy>=1.26",
    "aerocalc",
    "attitude3d",
    "flightdyn",
]

[project.optional-dependencies]
test = ["pytest>=8.0"]

[tool.hatch.build.targets.wheel]
packages = ["src/aircraftsim"]

[tool.uv.sources]
aerocalc = { workspace = true }
attitude3d = { workspace = true }
flightdyn = { workspace = true }
