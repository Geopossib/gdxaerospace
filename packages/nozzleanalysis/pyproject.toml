[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "nozzleanalysis"
version = "0.1.0"
description = "Isentropic rocket/jet nozzle flow analysis for the GDX Aerospace ecosystem"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "GDX Tech Co. Ltd" }]
dependencies = [
    "numpy>=1.26",
    "aerocalc",
    "compressibleflow",
]

[project.optional-dependencies]
test = ["pytest>=8.0"]

[tool.hatch.build.targets.wheel]
packages = ["src/nozzleanalysis"]

[tool.uv.sources]
aerocalc = { workspace = true }
aerounits = { workspace = true }
compressibleflow = { workspace = true }
