# Notebooks

Example Jupyter notebooks per discipline. `aerodynamics/` is scaffolded for
Phase 2; Phase 1 examples are plain scripts under `examples/` instead, since
Phase 1 has no plotting-heavy content yet.
