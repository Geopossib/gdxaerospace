# Benchmarks

Performance and numerical-accuracy benchmarks for GDX Aerospace packages
land here as they're added (Phase 2+). Phase 1 has no performance-critical
code path yet — `Atmosphere` evaluation is O(1) per call.
